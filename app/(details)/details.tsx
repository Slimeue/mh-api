import { Stack } from "expo-router";
import { View } from "react-native";
import { Text } from "react-native-paper";

const Page = () => {
  return (
    <View>
      <Stack.Screen />
    </View>
  );
};

export default Page;
